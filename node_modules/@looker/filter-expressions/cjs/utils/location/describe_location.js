"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.describeLocation = void 0;

var _defaultTo = _interopRequireDefault(require("lodash/defaultTo"));

var _describe_user_attribute = require("../user_attribute/describe_user_attribute");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

const describeLat = lat => `${Math.abs(lat).toFixed(1)}°${lat > 0 ? 'N' : 'S'}`;

const describeLon = lon => `${Math.abs(lon).toFixed(1)}°${lon > 0 ? 'E' : 'W'}`;

const box = ({
  lon,
  lat,
  lon1,
  lat1
}) => lon && lat && lon1 && lat1 ? `${describeLat(lat)}, ${describeLon(lon)} to ${describeLat(lat1)}, ${describeLon(lon1)}` : '';

const circle = ({
  distance,
  unit,
  lat,
  lon
}) => distance && unit && lat && lon ? `${distance} ${unit} from ${lat}, ${lon}` : '';

const location = ({
  lat,
  lon
}) => lat && lon ? `${lat}, ${lon}` : '';

const anyvalue = () => 'is anywhere';

const describeNull = () => 'is null';

const describeNotNull = () => 'is not null';

const filterToStringMap = {
  box,
  circle,
  location,
  anyvalue,
  null: describeNull,
  notnull: describeNotNull,
  user_attribute: _describe_user_attribute.describeUserAttribute
};

const describeLocation = item => (0, _defaultTo.default)(filterToStringMap[item.type], () => '')(item);

exports.describeLocation = describeLocation;
//# sourceMappingURL=describe_location.js.map
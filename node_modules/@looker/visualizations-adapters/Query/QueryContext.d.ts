/// <reference types="react" />
import type { IError } from '@looker/sdk';
import type { CAll, SDKRecord, Fields, RawApiConfigResponse, Totals } from '../types';
export interface QueryContextProps {
    ok: boolean;
    loading: boolean;
    config: Partial<CAll> | Partial<RawApiConfigResponse>;
    data: SDKRecord[];
    error?: IError;
    fields: Fields;
    totals?: Totals;
    shareUrl?: string;
}
export declare const QueryContext: import("react").Context<QueryContextProps>;

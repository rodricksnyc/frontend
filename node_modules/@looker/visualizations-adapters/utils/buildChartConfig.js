import { normalizeChartTypes, chartConfigByType, sanitizeSDKResponse } from '../transformations';
import flow from 'lodash/flow';
export var buildChartConfig = function buildChartConfig(args) {
  var _flow = flow([normalizeChartTypes, function (args) {
    var type = args.config.type;
    var configTransformations = chartConfigByType[type] || chartConfigByType["default"];
    return flow(configTransformations)(args);
  }, sanitizeSDKResponse])(args),
      config = _flow.config;

  return config;
};
//# sourceMappingURL=buildChartConfig.js.map
import format from 'date-fns/format';
import parseISO from 'date-fns/parseISO';
import { isDateQuery } from './isDateQuery';
export var formatDateLabel = function formatDateLabel(_ref) {
  var dateString = _ref.dateString,
      fields = _ref.fields;

  if (!isDateQuery(fields)) {
    return dateString;
  }

  var type = fields.dimensions[0].type;
  var dateObj = parseISO(dateString);

  if (type === 'date_year') {
    return format(dateObj, 'yyyy');
  } else if (type === 'date_month') {
    return format(dateObj, "MMMM \u2018yy");
  } else if (type === 'date_date' || type === 'date_week') {
    return format(dateObj, 'LLL d');
  } else {
    return dateString;
  }
};
//# sourceMappingURL=formatDateLabel.js.map
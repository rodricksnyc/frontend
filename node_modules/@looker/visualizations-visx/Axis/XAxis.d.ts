/// <reference types="react" />
import type { Fields } from '@looker/visualizations-adapters';
export declare type XAxisProps = {
    fields: Fields;
    label?: string;
    labelDy: number;
    showTicks?: boolean;
    tickAngle: number;
    tickTextAnchor: 'end' | 'start' | 'middle' | 'inherit';
    tickSpace: number;
};
export declare const XAxis: ({ fields, label, labelDy, showTicks, tickAngle, tickTextAnchor, tickSpace, }: XAxisProps) => JSX.Element;
